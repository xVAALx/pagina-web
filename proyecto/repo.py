import reflex as rx
from proyecto.components.navbar import navbar


def repo():
    return rx.box(
            navbar("/iconos/ybcde.svg"),
                    rx.vstack(
                        
                        ),
),